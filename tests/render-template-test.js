const renderTemplate = require('../lib/render-template')
    , buster         = require('bustermove')
    , assert         = require('referee').assert
    , refute         = require('referee').refute

buster.testCase('render-template', {
  
})